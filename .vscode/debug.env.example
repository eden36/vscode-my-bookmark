# 可选：指定本机 VS Code 可执行文件。留空时自动从 PATH 中的 code 命令解析。
# 用于 F5「在 VS Code 中调试/启动」、npm run dev:vscode 与集成测试。
# 复制本文件为 debug.env 并填写路径。
#
# 说明：在 Cursor 中按 F5 时，extensionHost 的 runtimeExecutable 会被忽略，
# 因此通过脚本启动独立 VS Code 窗口；「在 VS Code 中调试」会附加到 inspect 端口 29333。
#
# Windows 示例：
# MY_BOOKMARK_VSCODE_EXECUTABLE=D:\auto_software\Microsoft.VisualStudioCode\Code.exe
#
# 默认安装路径示例：
# MY_BOOKMARK_VSCODE_EXECUTABLE=C:\Users\<你>\AppData\Local\Programs\Microsoft VS Code\Code.exe
